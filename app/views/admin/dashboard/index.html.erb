<h1>All Users</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= user.email %></li>
    <%= link_to 'Show', admin_dashboard_path(user) %>
    <%= link_to 'Edit', edit_admin_dashboard_path(user) %>
  <% end %>
</ul>

<%= link_to 'Create New User', new_admin_dashboard_path %>